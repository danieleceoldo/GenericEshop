@page "/cart"
@rendermode InteractiveServer
@inject Cart Cart


<h2>Shopping Cart</h2>

@if (Cart.Items.Any())
{
    <ul>
        @foreach (var item in Cart.Items)
        {
            <li>
                <h3>@item.Product?.Name</h3>
                <p>Price: @item.Product?.Price.ToString("C")</p>
                <p>Quantity: @item.Quantity</p>
                <p>Total: @item.TotalPrice.ToString("C")</p>
                <button @onclick="() => Cart.Items.Remove(item)">Remove</button>
            </li>
        }
    </ul>
    <p><strong>Cart Total: @Cart.TotalPrice.ToString("C")</strong></p>
}
else
{
    <p>Your shopping cart is currently empty.</p>
}

<nav>
    <NavLink href="/products"> Continue Shopping </NavLink>
     @if (Cart.Items.Any())
     {
        <NavLink href="/checkout"> Proceed to Checkout </NavLink>
     }
</nav>